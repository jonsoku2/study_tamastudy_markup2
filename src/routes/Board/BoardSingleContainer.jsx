import React from 'react';
import PropTypes from 'prop-types';
import BoardSinglePresenter from './BoardSinglePresenter';

const BoardSingleContainer = props => {
  return <BoardSinglePresenter />;
};

BoardSingleContainer.propTypes = {};

export default BoardSingleContainer;
