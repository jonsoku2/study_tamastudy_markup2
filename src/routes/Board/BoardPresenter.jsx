import React from 'react';
import PropTypes from 'prop-types';

const BoardPresenter = props => {
  return <div>BoardPresenter</div>;
};

BoardPresenter.propTypes = {};

export default BoardPresenter;
